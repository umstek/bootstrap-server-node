{
  "name": "bootstrap-server-node",
  "version": "0.1.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "prestart": "yarn clean",
    "start": "ts-node --project tsconfig.json src/index.ts",
    "prebuild": "yarn clean",
    "build": "tsc",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "@types/node": "16.0.0",
    "ts-node": "10.2.1",
    "tslint": "6.1.3",
    "typescript": "4.9.5"
  },
  "dependencies": {
    "immutable": "^4.0.0-rc.12",
    "rxjs": "^7.1.0"
  }
}
