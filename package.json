{
  "name": "bootstrap-server-node",
  "version": "0.1.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "prestart": "yarn clean",
    "start": "ts-node --project tsconfig.json src/index.ts",
    "prebuild": "yarn clean",
    "build": "tsc",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "@types/node": "10.12.18",
    "ts-node": "7.0.1",
    "tslint": "5.12.0",
    "typescript": "3.9.9"
  },
  "dependencies": {
    "immutable": "^4.0.0-rc.12",
    "rxjs": "^6.3.3"
  }
}
